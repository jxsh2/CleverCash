{
  "name": "clevercash-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm run test"
  },
  "dependencies": {
    "mongodb": "^6.15.0",
    "react-scripts": "^5.0.1"
  }
}
